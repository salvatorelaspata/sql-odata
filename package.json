{
  "scripts": {
    "start": "platformatic start",
    "test": "node --test test/*/*.test.js",
    "migrate": "platformatic db migrations apply",
    "dev": "platformatic start --inspect"
  },
  "devDependencies": {
    "fastify": "^4.24.3"
  },
  "dependencies": {
    "@platformatic/db": "^1.10.0",
    "fastify-xml-body-parser": "^2.2.0",
    "platformatic": "^1.10.0"
  },
  "engines": {
    "node": "^18.8.0 || >=20.6.0"
  }
}
